apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fake-stackoverflow.fullname" . }}-test-connection"
  labels:
    {{- include "fake-stackoverflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fake-stackoverflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
